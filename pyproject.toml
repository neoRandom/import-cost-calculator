[project]
name = "import-cost-calculator"
version = "0.1.0"
description = "Calculates Brazilian import costs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
